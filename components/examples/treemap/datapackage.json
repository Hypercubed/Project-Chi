{
	"name": "Treemap",
	"title": "webtreemap",
	"description": "A simple treemap implementation using web technologies (DOM nodes, CSS styling and transitions) rather than a canvas/svg/plugin.",
	"resources": [
		{"path": "../../../bundles/app-bundle.tsv", "schema": "treemap-schema" },
		{"path": "../../../bundles/deps-bundle.tsv", "schema": "treemap-schema" }
	],
	"schemas": {
		"treemap-schema": {
			"fields": [
				{ "name": "Source", "type": "string", "constraints": { "required": true } },
				{ "name": "Size", "type": "number", "constraints": { "required": true } },
				{ "name": "Tag", "type": "string" }
			]
		}
	},
	"image": "thumbnail.png",
	"readme": "README.md"
}
